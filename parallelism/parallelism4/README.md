```text

This parallelism4.sh will implement the number of thread, process the number of threads and wait each thread to finish to start another one, always keeping the program running in parallel the thread number.

```
